<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhly.mapper.BannerMapper">
    
    <resultMap id="BaseResultMap" type="com.zhly.entity.Banner">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="subtitle" property="subtitle" />
        <result column="label" property="label" />
        <result column="image_url" property="imageUrl" />
        <result column="position" property="position" />
        <result column="link_type" property="linkType" />
        <result column="link_value" property="linkValue" />
        <result column="display_order" property="displayOrder" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="status" property="status" />
        <result column="click_count" property="clickCount" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    
    <!-- 获取启用的轮播图列表（按显示顺序排序） -->
    <select id="selectActiveBanners" resultMap="BaseResultMap">
        SELECT * FROM banner
        WHERE status = 1
        AND (start_time IS NULL OR start_time &lt;= NOW())
        AND (end_time IS NULL OR end_time &gt;= NOW())
        ORDER BY display_order ASC, create_time DESC
    </select>
    
    <!-- 根据位置获取启用的轮播图列表（按显示顺序排序） -->
    <select id="selectActiveBannersByPosition" resultMap="BaseResultMap">
        SELECT * FROM banner
        WHERE status = 1
        AND position = #{position}
        AND (start_time IS NULL OR start_time &lt;= NOW())
        AND (end_time IS NULL OR end_time &gt;= NOW())
        ORDER BY display_order ASC, create_time DESC
    </select>
    
</mapper>







