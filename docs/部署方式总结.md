# 部署方式总结

## 📋 部署方式一览

| 方式 | 难度 | 适用场景 | 推荐度 |
|------|------|----------|--------|
| **宝塔面板** | ⭐⭐ | 新手、个人项目 | ⭐⭐⭐⭐⭐ |
| **Docker** | ⭐⭐⭐⭐ | 生产环境、团队协作 | ⭐⭐⭐⭐⭐ |
| **传统手动部署** | ⭐⭐⭐ | 学习、定制需求 | ⭐⭐⭐ |
| **一键脚本部署** | ⭐⭐ | 快速部署 | ⭐⭐⭐⭐ |

---

## 方式一：宝塔面板部署（推荐新手）

### 适用场景
- 完全新手，不想敲命令
- 个人项目、小团队
- 需要图形化管理

### 优点
- ✅ 图形化界面，操作简单
- ✅ 一键安装软件
- ✅ 可视化管理文件
- ✅ 自动备份功能

### 缺点
- ❌ 占用一定服务器资源
- ❌ 需要额外学习面板操作

### 具体步骤

#### 1. 购买服务器
- 选择：腾讯云/阿里云
- 系统：Ubuntu 22.04 LTS
- 配置：2核4G，带宽3Mbps

#### 2. 安装宝塔面板
```bash
# 连接服务器
ssh root@你的服务器IP

# 安装宝塔（Ubuntu/Debian）
wget -O install.sh http://download.bt.cn/install/install-ubuntu_6.0.sh
sudo bash install.sh

# 安装完成后会显示：
# 外网面板地址: http://你的IP:8888/随机字符串
# 用户名和密码
```

#### 3. 登录面板并安装软件
1. 浏览器访问：`http://你的IP:8888/随机字符串`
2. 登录面板
3. 安装软件：
   - Nginx 1.20+
   - MySQL 8.0
   - Redis 7.0
   - Java 17（Spring Boot 3需要）

#### 4. 创建数据库
1. 面板 → 数据库 → 添加数据库
2. 数据库名：`travel_db`
3. 用户名：`travel_user`
4. 密码：设置强密码
5. 记录这些信息

#### 5. 部署后端
```bash
# 方式1：通过面板文件管理器
# 1. 面板 → 文件 → 进入 /www/wwwroot
# 2. 创建 travel-server 目录
# 3. 上传 travel-server.jar 文件

# 方式2：通过命令行
cd /www/wwwroot
mkdir travel-server
# 使用 scp 或 FTP 上传 jar 文件
```

创建启动脚本：
```bash
# 在面板文件管理器中，进入 travel-server 目录
# 创建 start.sh 文件，内容：

#!/bin/bash
cd /www/wwwroot/travel-server
nohup java -jar -Xms512m -Xmx1024m travel-server.jar \
  --spring.profiles.active=prod \
  > server.log 2>&1 &
echo $! > server.pid
```

设置权限并启动：
```bash
chmod +x start.sh
./start.sh
```

#### 6. 部署前端
1. **本地构建前端**
   ```bash
   cd travel-admin
   npm run build
   # 生成 dist 目录
   ```

2. **上传到服务器**
   - 将 `dist` 目录内容上传到 `/www/wwwroot/travel-admin`

#### 7. 配置 Nginx
1. 面板 → 网站 → 添加站点
2. 域名：输入你的域名或IP
3. 根目录：`/www/wwwroot/travel-admin`
4. 点击"设置" → "配置文件"，修改为：

```nginx
server {
    listen 80;
    server_name 你的域名或IP;
    
    # 前端静态文件
    location / {
        root /www/wwwroot/travel-admin;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端API代理
    location /api/ {
        proxy_pass http://127.0.0.1:8070;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    # 上传文件
    location /upload/ {
        alias /www/wwwroot/travel-server/upload/;
        expires 30d;
    }
}
```

5. 保存并重启 Nginx

#### 8. 配置环境变量
创建配置文件：
```bash
# 面板 → 文件 → /www/wwwroot/travel-server
# 创建 application-prod.yml
```

内容：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: travel_user
    password: 你的数据库密码
  data:
    redis:
      host: localhost
      port: 6379
      password: 
```

#### 9. 测试访问
- 前端：`http://你的IP`
- 后端API：`http://你的IP/api/health/check`

---

## 方式二：Docker 部署（推荐生产）

### 适用场景
- 生产环境
- 团队协作
- 需要环境一致性
- 微服务架构

### 优点
- ✅ 环境一致，开发=生产
- ✅ 快速部署和扩展
- ✅ 资源隔离，安全
- ✅ 易于版本管理

### 缺点
- ❌ 学习曲线较陡
- ❌ 需要理解容器概念

### 具体步骤

#### 1. 安装 Docker
```bash
# 连接服务器
ssh root@你的服务器IP

# 安装 Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 启动 Docker
sudo systemctl start docker
sudo systemctl enable docker

# 安装 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 验证安装
docker --version
docker-compose --version
```

#### 2. 准备项目文件
```bash
# 在服务器上创建项目目录
mkdir -p /opt/travel
cd /opt/travel

# 上传项目文件（使用 scp 或 git）
# 确保有以下文件：
# - travel-server/Dockerfile
# - travel-server/docker-compose.yml
# - travel-server/src/...
# - travel-admin/...
```

#### 3. 配置环境变量
```bash
cd /opt/travel/travel-server

# 创建 .env 文件
cat > .env << EOF
# 数据库配置
MYSQL_ROOT_PASSWORD=root_password_123
MYSQL_DATABASE=travel_db
MYSQL_USER=travel_user
MYSQL_PASSWORD=travel_password_123

# Redis配置
REDIS_PASSWORD=

# 端口配置
MYSQL_PORT=3306
REDIS_PORT=6379
BACKEND_PORT=8070
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
EOF
```

#### 4. 构建前端
```bash
# 在本地或服务器上
cd travel-admin
npm install
npm run build
# 生成 dist 目录
```

#### 5. 启动所有服务
```bash
cd /opt/travel/travel-server

# 使用一键部署脚本
chmod +x deploy/docker-deploy.sh
./deploy/docker-deploy.sh prod

# 或手动启动
docker-compose up -d
```

#### 6. 查看服务状态
```bash
# 查看容器状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f backend
```

#### 7. 测试访问
- 前端：`http://你的IP`
- 后端API：`http://你的IP/api/health/check`

#### 8. 常用管理命令
```bash
# 停止所有服务
docker-compose down

# 重启服务
docker-compose restart backend

# 查看资源使用
docker stats

# 进入容器
docker exec -it travel-backend /bin/bash
```

---

## 方式三：传统手动部署

### 适用场景
- 想深入学习 Linux
- 需要完全控制
- 简单项目
- 学习目的

### 优点
- ✅ 完全控制
- ✅ 学习价值高
- ✅ 资源占用少

### 缺点
- ❌ 配置复杂
- ❌ 容易出错
- ❌ 需要较多 Linux 知识

### 具体步骤

#### 1. 服务器准备
```bash
# 连接服务器
ssh root@你的服务器IP

# 更新系统
apt update && apt upgrade -y  # Ubuntu/Debian
# 或
yum update -y                  # CentOS

# 安装必要软件
apt install -y openjdk-17-jdk mysql-server redis-server nginx
```

#### 2. 配置数据库
```bash
# 启动 MySQL
systemctl start mysql
systemctl enable mysql

# 登录 MySQL
mysql -u root -p
# 输入 root 密码

# 创建数据库和用户
CREATE DATABASE travel_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'travel_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON travel_db.* TO 'travel_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### 3. 配置 Redis
```bash
# 启动 Redis
systemctl start redis
systemctl enable redis

# 测试 Redis
redis-cli ping
# 应该返回 PONG
```

#### 4. 部署后端
```bash
# 创建目录
mkdir -p /opt/travel-server
cd /opt/travel-server

# 上传 jar 文件（使用 scp）
# scp travel-server.jar root@服务器IP:/opt/travel-server/

# 使用项目提供的启动脚本
chmod +x deploy/*.sh
./deploy/start.sh prod
```

#### 5. 配置后端环境
```bash
# 创建配置文件
vim /opt/travel-server/application-prod.yml
```

内容：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: travel_user
    password: your_password
  data:
    redis:
      host: localhost
      port: 6379
```

#### 6. 部署前端
```bash
# 创建目录
mkdir -p /opt/travel-admin

# 上传前端文件（dist 目录内容）
# 使用 scp 或 FTP
```

#### 7. 配置 Nginx
```bash
# 创建配置文件
vim /etc/nginx/sites-available/travel
```

内容：
```nginx
server {
    listen 80;
    server_name 你的域名或IP;

    # 前端
    location / {
        root /opt/travel-admin;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端API
    location /api/ {
        proxy_pass http://127.0.0.1:8070;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 上传文件
    location /upload/ {
        alias /opt/travel-server/upload/;
        expires 30d;
    }
}
```

启用配置：
```bash
# 创建软链接
ln -s /etc/nginx/sites-available/travel /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

#### 8. 设置开机自启
```bash
# 创建 systemd 服务
cat > /etc/systemd/system/travel-server.service << 'EOF'
[Unit]
Description=Travel Server Application
After=network.target mysql.service redis.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/travel-server
ExecStart=/usr/bin/java -jar -Xms512m -Xmx1024m travel-server.jar --spring.profiles.active=prod
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 启用服务
systemctl daemon-reload
systemctl enable travel-server
systemctl start travel-server
systemctl status travel-server
```

#### 9. 配置防火墙
```bash
# Ubuntu/Debian
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 8070/tcp
ufw enable

# CentOS
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --permanent --add-port=8070/tcp
firewall-cmd --reload
```

#### 10. 测试访问
- 前端：`http://你的IP`
- 后端：`http://你的IP:8070`
- API：`http://你的IP/api/health/check`

---

## 方式四：一键脚本部署

### 适用场景
- 快速部署
- 标准化环境
- 重复部署

### 优点
- ✅ 快速简单
- ✅ 自动化
- ✅ 减少错误

### 缺点
- ❌ 需要先准备脚本
- ❌ 灵活性较低

### 具体步骤

#### 1. 使用项目提供的脚本
```bash
# 上传项目到服务器
cd /opt/travel/travel-server

# 赋予执行权限
chmod +x deploy/*.sh

# 启动服务
./deploy/start.sh prod

# 查看状态
./deploy/status.sh

# 停止服务
./deploy/stop.sh

# 重启服务
./deploy/restart.sh prod
```

#### 2. 使用 Docker 一键部署
```bash
cd /opt/travel/travel-server

# 使用 Docker 部署脚本
chmod +x deploy/docker-deploy.sh
./deploy/docker-deploy.sh prod
```

---

## 📊 部署方式对比表

| 特性 | 宝塔面板 | Docker | 传统部署 | 一键脚本 |
|------|---------|--------|---------|---------|
| **难度** | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **学习成本** | 低 | 高 | 中 | 低 |
| **部署速度** | 快 | 快 | 慢 | 最快 |
| **环境一致性** | 中 | 高 | 低 | 高 |
| **可扩展性** | 低 | 高 | 中 | 中 |
| **资源占用** | 高 | 中 | 低 | 中 |
| **适合新手** | ✅ | ❌ | ❌ | ✅ |
| **适合生产** | ⚠️ | ✅ | ⚠️ | ✅ |

---

## 🎯 选择建议

### 如果你是新手
**推荐：宝塔面板**
- 图形化操作，简单直观
- 不需要记命令
- 适合个人项目

### 如果是生产环境
**推荐：Docker**
- 环境一致
- 易于扩展
- 标准化部署

### 如果想学习
**推荐：传统部署**
- 完全控制
- 学习价值高
- 理解底层原理

### 如果想快速部署
**推荐：一键脚本**
- 自动化程度高
- 减少错误
- 快速上线

---

## 📝 部署检查清单

### 部署前
- [ ] 购买云服务器
- [ ] 选择操作系统（推荐 Ubuntu 22.04）
- [ ] 开放必要端口（80, 443, 8070, 3306, 6379）
- [ ] 配置 SSH 密钥登录

### 环境准备
- [ ] 安装 Java 17
- [ ] 安装 MySQL 8.0
- [ ] 安装 Redis
- [ ] 安装 Nginx（或使用 Docker）

### 应用部署
- [ ] 构建前端：`npm run build`
- [ ] 打包后端：`mvn clean package`
- [ ] 上传文件到服务器
- [ ] 配置数据库连接
- [ ] 配置 Redis 连接
- [ ] 配置 Nginx 反向代理

### 部署后
- [ ] 后端服务是否启动
- [ ] 前端页面是否正常访问
- [ ] API 接口是否正常
- [ ] 数据库连接是否正常
- [ ] 文件上传功能是否正常
- [ ] 日志是否正常记录

---

## 🚀 快速开始

### 最快方式（宝塔）
1. 安装宝塔面板
2. 安装软件（Nginx、MySQL、Redis、Java）
3. 上传文件
4. 配置 Nginx
5. 完成！

### 最标准方式（Docker）
1. 安装 Docker
2. 配置环境变量
3. 运行 `docker-compose up -d`
4. 完成！

---

## 📚 相关文档

- [部署知识完全指南.md](./部署知识完全指南.md) - 详细知识
- [部署快速入门.md](./部署快速入门.md) - 快速入门
- [部署概念通俗解释.md](./部署概念通俗解释.md) - 概念解释

---

**记住：选择适合你的方式，从简单开始，逐步深入！** 🎉












